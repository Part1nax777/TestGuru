<% if logged_in? %>
  <p class="nav user">Добро пожаловать, <b><%= current_user.name %></b> Guru</p>
  <nav>
    <ul class="nav bar navbar-nav">
      <%= link_to 'TestGuru', root_path %>
      <%= link_to 'Выход', logout_path, method: :delete %>
    </ul>
  </nav>
  <% else %>
  <nav>
    <ul class="nav bar navbar-nav">
      <%= link_to 'Регистрация', signup_path %>
      <%= link_to 'Авторизация', login_path %>
    </ul>
  </nav>
<% end %>
